# 給 Claude Code 的專案說明

## 📌 目前的讀書計畫（每次接手先看這裡）

| 項目 | 內容 |
|---|---|
| 考試日 | **2027 年 1 月 22 日**（學測） |
| 計畫起點 | 2026 年 8 月 10 日，全程 165 天 |
| 學習期 | 前 100 天（到 2026/11/18）上新字，**每天 62 個** |
| 鞏固期 | 後 65 天不上新字，只複習 |
| 考前 10 天 | 在 App 課表按「啟動考前總複習」，把所有字重排一次 |
| 目標範圍 | **官方 1～5 級共 5091 字**（2026/08/31 改的：先以 1～5 級為主）。第 6 級那 1023 字最冷僻，暫時不收；要全包就到 App 的「課表 → 目標範圍」切成「全部 6 級」 |

### 字庫進度（每寫完一批就更新這一列）

**已編好釋義與例句：4888 / 6114（8002 個字義）**

- `w1`～`w5`：440 字（原始批次，涵蓋 1～5 級）
- `w6`：47 字（第 3 級，動詞為主）
- `w7`：61 字（第 3 級，形容詞）
- `w8`：55 字（第 3 級，抽象名詞 A–M）
- `w9`：45 字（第 3 級，抽象名詞 N–Z）
- `w10`：52 字（第 3 級，動詞與多詞性字）
- `w11`：59 字（第 3 級，形容詞、動詞與名詞）
- `w12`：116 字（第 3 級，副詞、介系詞與多詞性動詞名詞 A–P）
- `w13`：125 字（第 3 級 R–Y 收尾 67 字 ＋ 第 4 級 A–B 起頭 58 字）
- `w14`：127 字（第 4 級：A–B 補遺 20 字 ＋ C 開頭 107 字，做到 `curve`）
- `w15`：125 字（第 4 級 D–F，做到 `freshman`）
- `w16`：122 字（第 4 級 G–L，做到 `luxury`）
- `w17`：123 字（第 4 級 M–P，做到 `pursuit`）
- `w18`：114 字（第 4 級 Q–T，做到 `timetable`）
- `w19`：121 字（第 4 級 T–Z 收尾 ＋ 前段補遺）
- `w20`：118 字（第 5 級 A–C，做到 `cluster`）
- `w21`：116 字（第 5 級 C–D，做到 `document`）
- `w22`：120 字（第 5 級 D–H，做到 `hazard`）
- `w23`：129 字（第 5 級 H–N，`heir` 到 `notion`）
- `w24`：125 字（第 5 級 A–G 漏網 ＋ O–R，做到 `recruit`）
- `w25`：126 字（第 5 級 R 尾端 ＋ S–T，做到 `transition`）
- `w26`：122 字（第 5 級收尾：S 補遺 ＋ T–Z，做到 `worthy`）
- `w27`：134 字（第 5 級完結：剩餘冷僻名詞全收，**第 5 級 1006/1007 完成**）
- `w28`：134 字（**第 2 級起頭** A–C，做到 `cheat`）
- `w29`：130 字（第 2 級 C–E，做到 `electric`）
- `w30`：130 字（第 2 級 E–H，做到 `hurry`，**突破官方詞彙表一半**）
- `w31`：130 字（第 2 級 H–O，做到 `opinion`）
- `w32`：130 字（第 2 級 O–S，做到 `scared`）
- `w33`：130 字（第 2 級 S–T，做到 `till`）
- `w34`：111 字（**第 2 級完結 1033/1033** ＋ 第 4 級漏網 18 字）
- `w35`：120 字（**第 1 級起頭** A–B，做到 `bread`）
- `w36`：120 字（第 1 級 B–D，做到 `dollar`）
- `w37`：120 字（第 1 級 D–G，做到 `goddess`）
- `w38`：120 字（第 1 級 G–L，做到 `learn`）
- `w39`：117 字（第 1 級 L–N，`least` 到 `nurse` 全收）
- `w40`：120 字（第 1 級 O–R，`of` 到 `rich`，**突破 70%**）
- `w41`：121 字（第 1 級 R–S，`ride` 到 `strong`）
- `w42`：77 字（第 1 級 S–T，`student` 到 `touch`）
- `w43`：89 字（第 1 級 T–Z，`towel` 到 `zoo`，**第 1 級完結 1010/1014**）
- `w44`：133 字（第 3 級 A–M 的可考字：`adventure` 到 `mushroom`）
- `w45`：133 字（第 3 級 N–Z 的可考字 ＋ A–M 漏網的 14 個，**1～5 級的可考字到此做滿**）
- `p1`：52 個片語

**下一批要做**：**沒有了。1～5 級值得收的字已經全部做完。**
2026/09/06 做完 `w45` 之後，第 3 級 N–Z 的可考字也收齊了，
剩下的 120 個第 3 級與 78 個第 4 級**全是冷僻具體名詞**
（kangaroo、spaghetti、doughnut、lollipop、zipper、napkin、saucer、yolk 這類），
學測不會考。**除非使用者明確要求，不要再補字。**

| 分級 | 還缺 | 性質 |
|---|---|---|
| 第 1 級 | 4 字 | `mister`／`nope`／`ok`／`yeah`，刻意不收（口語變體） |
| 第 2 級 | 0 | 完結 |
| 第 3 級 | 120 字 | 全是冷僻具體名詞 |
| 第 4 級 | 78 字 | 全是冷僻具體名詞（aquarium、broom、lobster、pasta、raisin 這類） |
| 第 5 級 | 1 字 | `ass`，刻意不收（見下面） |
| 第 6 級 | 1023 字 | 目標範圍外 |

目標範圍是 1～5 級的話，字庫已經覆蓋 4888／5091 ＝ **96%**，
而且缺的那 203 個全部是不會考的字，**內容已經不是瓶頸**。

還想動字庫的話，優先序是：
1. 什麼都不做——8002 個字義遠超過學習期 100 天 × 每天 70 個 ＝ 7000 的需求。
2. 真的要衝數量，才做第 3、4 級剩下的具體名詞（用下面的 `todo.py` 列清單）。
3. 第 6 級 1023 字在目標範圍外，除非使用者把「課表 → 目標範圍」切成全部 6 級。

用下面的腳本就能列出待補清單：

```bash
python .claude/skills/add-words/scripts/todo.py 3 130
```

**舊例句改寫：已完工（2026/09）**。`w1`～`w5` 與 `p1` 原本有 987 句短於 10 字，
情境線索不足，現在**全部改寫完畢**。全庫 12099 句例句，短於 10 字的 **0 句**，
平均 11.29 個英文字，每個資料檔都通過 `check.py`。

改寫方式（之後若有類似需求照這個做）：用 `check.py` 列出短句 →
逐句重寫成 10～14 字並重寫中譯 → 做成 `(舊英文, 新整行)` 的對照表，
用腳本比對英文部分整行替換。腳本會驗證每一句都確實命中、沒漏也沒重複比對，
所以不會改錯行。一次處理一個檔比較好驗收。

---

## 📋 剩下要做的事（2026/09 盤點）

### A. 字庫內容

| 分級 | 還缺 | 性質 | 建議 |
|---|---|---|---|
| 第 1 級 | 4 字 | `mister`／`nope`／`ok`／`yeah` | **刻意不收**，口語變體 |
| 第 2 級 | 0 | — | 完結 |
| 第 3 級 | 120 字 | 全是冷僻具體名詞（kangaroo、doughnut、zipper…） | **不必補**，學測不考 |
| 第 4 級 | 78 字 | 同上（aquarium、broom、lobster…） | 同上 |
| 第 5 級 | 1 字 | `ass` | **刻意不收**（只剩粗俗義） |
| 第 6 級 | 1023 字 | 目標範圍外 | 除非使用者把課表的目標範圍切成「全部 6 級」 |

**目標範圍（1～5 級）的覆蓋率是 4888／5091 ＝ 96%，內容已經不是瓶頸了。**
學習期 100 天 × 每天 70 個新字義 ＝ 7000，字庫有 8002 個字義，**整個計畫都夠用**。
`w45`（2026/09/06）之後**字庫工作正式收工**，接下來的重點在程式面與使用者自己的快篩。

### B. 程式面待辦

1. ~~`applyPlan2608` 是一次性遷移~~ **已於 2026/09/06 刪掉**。
2. **用真實資料校正 `Q_PER_UNIT` / `SEC_PER_UNIT`**（目前是 5.0 題／71 秒，
   來自 FSRS 模擬）。等使用者累積兩週紀錄後，用「紀錄」頁的實際題數與分鐘
   反推，把這兩個常數換成他自己的數字。
3. **兩週後檢視每日額度**：看紀錄頁的「每天實際分鐘」與「連續幾天沒做完」，
   決定 `perDay` 要不要從 70 往下調。判斷準則寫在對話裡：
   穩定超過 90 分鐘撐不住就降，錯題本沒在減少就把每天清的量從 20 提到 40。

### C. 提過但還沒做的改善（使用者尚未決定要不要做）

- ~~錯題優先清最近答錯的~~、~~錯題段分開顯示~~ **已於 2026/09/05 完成**，
  見下面「排序與畢業機制大改版」。
- **答錯後的例句預設收合**：現在 `allExamplesHTML()` 會把該字所有義項的例句
  一次全攤開，那是「答錯一題要 28 秒」的主因。收合起來、想看再點，
  省的是純閱讀時間，不太傷記憶。

### D. 使用者自己要做的（不是程式的事）

- **快篩第 1、2 級**：那是唯一能同時減少「每天題數」與「錯題產生量」的動作，
  而且做一次就永久有效。共 2047 字，他大概有一半以上看一眼就知道會。
- 在課表把「每天最少學幾個新字」與「學完日」調到他決定的路線。

---


### ⚠ 內容進度是整個計畫的瓶頸

使用者每天要學 62 個字，但一次對話大約只能產出 130～150 個高品質詞條。
**內容必須跑在學習進度前面**，否則他會沒有字可以練。
使用者的做法是每天開新對話叫你補一批——所以這份文件必須隨時保持最新，
新的對話才接得下去。**每寫完一批，務必回來更新上面的「字庫進度」。**

## 在新對話接手時怎麼開始

**最快的方式是直接叫用 `add-words` skill**（使用者說「繼續補單字」就會觸發）。
它把整套流程、查證方式、寫作規則與驗收都包好了，位置在
`.claude/skills/add-words/`，裡面附了兩個腳本：

```bash
python .claude/skills/add-words/scripts/todo.py        # 看還缺哪些字
python .claude/skills/add-words/scripts/todo.py 3 130  # 列第 3 級待補清單（含官方詞性）
python .claude/skills/add-words/scripts/check.py w8.js # 補完之後驗收
```

不用 skill 手動做的話，順序是：

1. 讀這份文件（尤其是上面的計畫與進度、下面的三項檢查）
2. 從「下一批要做」挑約 130 個字
3. 照「資料格式」與「寫例句的規則」寫進**新的資料檔**（`w8.js`、`w9.js`…）
4. 在 `index.html` 與 `sw.js` 各加一行，`sw.js` 的 `CACHE` 版本號 +1
5. 跑完下面**三項檢查**與 `check.py`
6. 回來更新「字庫進度」那一段


這是一個單人使用的英文單字練習網頁（純前端、離線、無後端）。
使用者是台灣高中生，目標是學測 7000 單與常考片語，程度請設定在**高中生看得懂**。

## 使用者最常要求的事：把新單字加進字庫

### 字庫檔案

| 檔案 | 內容 |
|---|---|
| `data/official.js` | **大考中心官方 6114 詞條的索引**（分級＋官方詞性，無釋義例句）。由 PDF 產生，不要手改 |
| `data/w1.js` | 單字 A–C |
| `data/w2.js` | 單字 D–F |
| `data/w3.js` | 單字 G–M |
| `data/w4.js` | 單字 N–R |
| `data/w5.js` | 單字 S–Z |
| `data/w6.js`～`data/w45.js` | 補充批次。`w6`～`w13` 前半第 3 級，`w13`～`w19` 第 4 級，`w20`～`w27` 第 5 級，`w28`～`w34` 第 2 級，`w35`～`w43` 第 1 級，`w44`／`w45` 回頭補第 3 級 |
| `data/p1.js` | 片語 |

**大批擴充時開新檔**（`w7.js`、`w8.js`…），不要用腳本插進既有檔案——
理由見下面「用腳本批次改資料檔的陷阱」。開新檔記得在 `index.html` 與 `sw.js`
各加一行，並把 `sw.js` 的 `CACHE` 版本號 +1，否則手機會拿到舊的快取。

目前進度：官方 6114 個字裡，**已編好釋義與例句的有 4888 個**（以上面的「字庫進度」為準）。
剩下的字在 App 的「查單字」查得到分級與詞性，但標示為「尚未編寫例句」。
擴充字庫就是把這些字逐批補成完整詞條——這是本專案接下來最主要的工作。

新字**依字母加到對應的檔案**，插在正確的字母順序位置。單一檔案超過約 150 個字時，
再開 `w6.js` 之類的新檔，並記得在 `index.html` 與 `sw.js` 的清單裡各加一行。

### 資料格式（定義在 `data/bank.js`）

```
@abandon|4
=v.|拋棄；遺棄
.The crew had to {{abandon}} the sinking ship as it began to tilt.|船身開始傾斜時，船員們只好棄船逃生。
.She would never {{abandon}} a close friend who was in serious trouble.|她絕不會拋下陷入嚴重麻煩的好朋友。
=v.|放棄（計畫、想法）
.He {{abandoned}} his plan to study abroad after his father fell ill.|父親病倒後，他放棄了出國念書的計畫。
```

- `@` 開頭：單字或片語，後面用 `|` 接**大考中心《高中英文參考詞彙表》的分級 1～6**。
  `0` 代表官方詞彙表查無此字；**片語不標分級**（官方 108 年版已不收片語）。
  含空格會被自動判定為片語。
- `=` 開頭：一個詞性與中文意思，用 `|` 隔開。**一個字有幾個常考意思就寫幾個 `=`**
- `.` 開頭：例句，用 `|` 隔開英文與中文。要挖空的字用 `{{ }}` 包起來

### 詞性一定要對照官方詞彙表

大考中心詞彙表的 PDF 放在專案根目錄（已被 `.gitignore` 排除，不會上傳）。
**新增或修改任何字之前，先用它確認該字的官方詞性與分級**，不要憑印象寫。
抽取分級的腳本寫法參考本文件最後一節。

原則：官方標了幾個詞性，就盡量把那幾個詞性的常用義項都寫進去。
若某個詞性在現代英文已罕用或過時（例如 `unique` 的 n.、`steady` 的 adv./n.），
可以不收，但要在此處記錄原因。

**目前刻意不收的**：`unique` n.（非現代標準用法）、`steady` adv./n.（過時）、
`scatter` n. 與 `grab` n.（口語且罕用，學測不會考）、
`rough` adv./n.（sleep rough 是英式且限於「露宿街頭」，n. 指草圖，兩者都罕用）、
`plenty` adv.（美式口語 plenty big enough，學測不考）、
`cease` n.（只出現在文言的 without cease）、
`disorder` v.（現代英文已不用，只剩過去分詞 disordered）、
`ethnic` n.（「少數族裔的一員」是過時用法，學測只考形容詞）、
`universal` n.（朗文只有電影公司這個專有名詞）、
`external` n. 與 `exotic` n.（都很文言，學測只考形容詞）。

`w23`（第 5 級 H–N）新增的：`incentive` adj.（只當名詞修飾語用，不是真形容詞）、
`initiate` adj./n. 與 `initiative` adj.（「新加入者」是宗教團體的舊用法）、
`league` v.（結盟）、`legitimate` v.（使合法）、`marine` n.（海軍陸戰隊員）、
`masculine` n.、`metropolitan` n.、`mock` n.（英式的模擬考複數 mocks）、
`neutral` n.（排檔的空檔）。

`w24`（第 5 級 A–G 漏網＋O–R）新增的：`certificate` v.（現代只用過去分詞
certificated）、`overall` n.（英式的工作罩衫）、`prior` n.（修道院副院長）、
`overturn` n.（罕用）、`pension` v.（只有 pension somebody off 這個片語）、
`perspective` adj. 與 `recipient` adj.（都是專門術語）、
`proportion` v.（只用被動的 be proportioned to，很文言）。

`w25`（第 5 級 R 尾端＋S–T）新增的：`regardless` adj.（古典用法）、
`retail` adv.（sell retail 是商業行話）、`sole` v.（給鞋換底，已是古語）、
`soak` n.、`steer` n.（閹牛）、`texture` v.、`sneak` n.（英式口語「打小報告的人」）。

`w26`（第 5 級收尾）新增的：`saint` v.（封聖）、`warehouse` v.（入倉）、
`slap` adv.（口語強調）、`verbal` n.（文法術語「動狀詞」）、
`whatsoever` pron.（古語）、`triple` n.（棒球三壘打）、`transit` v.、
`torch` v.（縱火，口語）。

`w27`（第 5 級完結）新增的：`canvas` v.、`dome` v.、`jug` v.、`ridge` v.、
`rim` v.、`yacht` v.（都極罕用）、`constitutional` n.（保健散步，古語）、
`midst` prep.、`foul` adv.（朗文沒有獨立義項，只有 foully）、
`rib` v.（取笑，口語）、`tin` v.（英式裝罐）、`nowhere` pron.（朗文只列副詞）、
`salmon` adj.（鮭魚色）、`nutrient` adj.（只當名詞修飾語）。

**整個字跳過的**：`ass`（第 5 級唯一未收的字）。官方標 n.，
但現代英文只剩粗俗義與過時的「驢子」，不適合放進高中生字庫。
第 5 級因此停在 1006／1007。

`w28`（第 2 級 A–C）新增的：`absent` v.（只有反身的 absent yourself from，
非常正式）、`cartoon` v.（朗文沒有獨立動詞條目）。

`w30`（第 2 級 E–H）新增的：`fit` adv.（朗文沒有獨立條目，副詞形式是 fittingly）。

`w32`（第 2 級 O–S）新增的：`post` adv.（朗文沒有獨立條目，
post-1990 那種是構詞前綴而非副詞）。

`w33`（第 2 級 S–T）新增的：`sour` n.（只有調酒名 whisky sour，學測不考）。

`w34`（第 2 級完結＋第 4 級漏網）新增的：`true` adv.（技術用語 aim true）
與 `true` v.（現代英文幾乎不存在）、`intermediate` v.（調停，極罕用）、
`scold` n.（愛責罵的人，古語）。

`w35`（第 1 級 A–B）新增的：`best` v.（擊敗，朗文標為 old-fashioned）。

`w44`（第 3 級 A–M）新增的：`downtown` n.（朗文只列副詞與形容詞，
downtown Miami 那種是形容詞用法）、`heel` v.（給鞋換後跟／緊跟在後，現代英文罕用）、
`knit` n.（朗文沒有獨立名詞條目，針織衣物的名詞是 knitwear）。

`w45`（第 3 級 N–Z）新增的：`parcel` v.（朗文只有 parcel out／up／off 三個片語動詞，
動詞原形不單用）、`fairy` adj.（朗文沒有形容詞條目，fairy tale 是名詞當修飾語）、
`underlying` v.（官方標的是 underlie 的現在分詞，但朗文只有形容詞條目 underlying）。
**整個字跳過的**：`queer`（朗文標「奇怪的」為 old-fashioned，另一義是敏感用語）、
`mike`（就是 microphone 的口語縮寫）。

`w36`（第 1 級 B–D）新增的：`but` adv.（＝only，文言）、`dear` adv.（cost dear）、
`course` v.（液體奔流，罕用）、`clean` n.（英式的 give it a clean）。

`w39`（第 1 級 L–N）新增的：`light` adv.（朗文沒有獨立副詞條目，只有 travel light
這個片語）、`machine` v.（用機器加工，是工業術語）。

`w40`（第 1 級 O–R）新增的：`out` 只收 adv.——prep. 是美式口語（look out the
window，英式教學視為錯誤）、adj. 朗文只在商業詞典有、n.（藉口／棒球出局）罕用、
v. 是「揭露某人是同志」不適合放進高中生字庫；`people` v.（只有被動的 be peopled
by，文學用法）、`photo` v.（口語，標準用法是 photograph）、`pig` v.（pig out 是
片語，動詞原形不單用）。
**整個字跳過的**：`nope`（＝no 的口語）、`mister`（就是 Mr. 的口語稱呼）、
`ok`（與 `okay` 同一個字，只是拼法不同，已收 `okay` 的四個詞性）。

`w42`／`w43`（第 1 級 S–Z 收尾）新增的：`sun` v.（只有反身的 sun yourself）、
`towel` v.（towel yourself dry，罕用）、`weekend` v.（度週末，英式且罕用）、
`while` v.（只有 while away 這個片語）、`without` adv.（go/do without 是片語）、
`where` n. 與 `why` n.（朗文沒有這兩個名詞條目，只出現在 the where and the when、
the whys and wherefores 這種並列說法裡）。
**整個字跳過的**：`yeah`（＝yes 的口語）。第 1 級因此停在 1010／1014，
另外三個是 `mister`／`nope`／`ok`。

`w41`（第 1 級 R–S）新增的：`size` v.（只有 size up 這個片語，動詞原形不單用）、
`small` adv.（chop it small，罕用）與 n.（the small of your back 腰部，解剖用語）、
`straight` n.（跑道的直線道，賽馬與田徑術語）。
另外 `row` 只收「一排」與「划船」——「爭吵」那個義項是英式且發音不同（/raʊ/），
官方只標 n./v.，不必硬收。

以上都已同步寫進 `check.py` 的 `SKIP_POS`，驗收不會再報這些缺漏。

**因為官方詞性湊不出現代用法而整個跳過的字**：`cable` v.（只有「發電報」這個舊義）、
`fist` v.（現代英文沒有這個動詞）、`addict` v.（現代只用 be addicted to，動詞原形不用）、
`counter` adj./adv.（只存在於 run counter to 這一個片語，硬寫會變成兩個重複的義項）、
`choke` n.（只有汽車的「阻風門」這個義項，學測不會考）。
要收的話得先想好那個詞性的義項怎麼寫。

### 品質優先序（使用者明確指定）

1. **中文釋義最重要**：要正式、要準確、一字多義要收齊。
   寫之前先查 <https://www.ldoceonline.com/dictionary/該字> 確認義項清單，
   釋義對應到朗文的哪一個 sense，不要憑印象翻譯。
2. **例句以「用法正確」為底線**：搭配詞、介系詞、接不定詞或動名詞都必須對，
   照朗文列出的 grammar pattern（例如 `assist somebody in doing something`）造句。
   文采不重要，用法錯了才是致命傷。

**已經踩過的錯**：`engage` 曾被我寫成「使訂婚」並用 `They got engaged` 當例句——
那是形容詞 `engaged` 的用法，朗文的動詞條目沒有這個義項。
**教訓：不要把某個變化形的慣用法，當成原形動詞的義項。**

### 寫例句的規則

1. **每個 `=` 至少 2 句例句**（練習時會輪流出現，避免使用者只記得住單一句子）。
   次要／冷門的字義可以只給 1 句。
2. `{{ }}` 裡面放**句子裡實際出現的形式**，包含變化形：
   `{{abandoned}}`、`{{looking forward to}}`、`{{was about to}}`。
   系統直接拿括號裡的字串當答案比對（不分大小寫、空白寬鬆）。
3. **句子長度一律 10～14 個英文單字。** 這是硬性要求：
   太短的句子（例如 `The surgeon performed the operation.`）幾乎沒有給出情境線索，
   使用者只能靠中文提示硬背，情境記憶法就失效了。
   句子要提供**足以回想出目標字的上下文**，例如加上原因、結果、時間或對比。
4. 英文要**道地而正式**：完整句、正確搭配詞、避免口語縮寫與俚語。
   不確定某個搭配是否自然時，去 <https://www.ldoceonline.com/dictionary/該字> 查證。
5. **句子裡其他的字要比目標字簡單**，不要用更難的字去解釋難字。
6. 中文翻譯用**台灣用語的繁體中文**。標點用全形（，。？！），不要出現任何簡體字。
7. 中文釋義比照學測 7000 單字表的常見譯法，多個意思用全形分號 `；` 隔開。
8. 內容裡不要出現 `|` 這個符號（它是欄位分隔符）。

### ⚠ 撞題：兩個字不可以共用同一句同一個中譯

寫例句時**不要照抄別的字的句子再換掉那個字**。同義詞尤其危險：

    It took great {{courage}} to admit the mistake in front of everyone.
    It took great {{bravery}} to admit the mistake in front of everyone.

這兩題長得一模一樣、中譯一模一樣，使用者填 courage 或 bravery 都對，
系統卻只認一個——等於**填出正確英文還被判錯**，那個字還會被丟進錯題本。

救得回來的情況：練習畫面的底線會顯示答案的**字母數**
（`renderCard()` 裡的 `"_".repeat(ans.length)`），
所以 `trash`(5) 與 `garbage`(7) 分得出來。字母數也一樣就完全無解。

`check.py` 的「F 撞題」會擋：**骨架相似 ≥0.97、中譯相似 ≥0.95、答案字母數相同**。
門檻抓得嚴是故意的——中譯差一兩個字通常正好差在答案上
（豬肉／牛肉、東半部／西半部），那種中文提示分得出來，不該擋。

2026/09 一次修掉 15 句：courage/bravery、sort/kind、supper/dinner、
server/waiter、nearly/almost、got/grew/became、but/except、garbage/trash、
countryside/country、dull/boring、flat/apartment、likelihood/chance、
marriage/wedding。

### ⚠ 每做完一批，一定要跑這三項檢查（使用者明確要求）

**1. 有沒有漏掉易考的多義字？**

兩層都要查：
- **詞性層**：跑下面的自動檢查，官方標了哪些詞性就要有對應義項。
- **義項層（自動檢查抓不到，一定要人工做）**：同一個詞性底下可能有多個常考意思。
  打開 <https://www.ldoceonline.com/dictionary/該字> 逐條對照，
  問自己「這個 sense 學測會不會考？」會考就要收。
  這一層踩過的實例：`loose` 漏了「掙脫的」、`solid` 漏了「純的；實心的」、
  `bitter` 的「憤恨的」與「嚴寒的」差點只寫「味苦的」。
  判定罕考而跳過的要在上面「刻意不收」那一節記錄理由。

**2. 中文解釋是否精闢正確？**

- 每個 `=` 的中文必須對應到朗文的某一個 sense，不要憑印象翻譯。
- 用**正式**的字典體釋義，不要口語。多個意思用全形分號 `；` 隔開。
- 檢查有沒有把某個變化形的慣用法誤當成原形的義項
  （踩過：`engage` 被寫成「使訂婚」，那其實是形容詞 `engaged`）。

**3. 句子的用法、語法、文法是否正確？**

- 搭配詞、介系詞、接不定詞或動名詞，一律照朗文列出的 grammar pattern
  （例如 `assist somebody in doing something`）。
- 逐句重讀一遍，特別留意冠詞、單複數、時態一致、以及不自然的搭配
  （踩過：`leave a safe job for art` 應為 `for a career in art`）。
- 中文翻譯裡不可以出現英文字（踩過兩次：`整座村莊completely被雪掩埋`、`偏遠的山village`）。

### 加完之後一定要做的檢查

用瀏覽器打開 `index.html`（或 preview_start 起 `vocab`），**先看 console 有沒有紅字**，再執行：

```js
JSON.stringify({
  total: WORD_BANK.length,
  dup: (function(){var s={},d=[];WORD_BANK.forEach(e=>{var k=e.w.toLowerCase();if(s[k])d.push(e.w);s[k]=1});return d})(),
  noBlank: WORD_BANK.flatMap(e=>e.s.flatMap(s=>s.ex)).filter(x=>!/\{\{.+?\}\}/.test(x.en)).length,
  emptySense: WORD_BANK.filter(e=>e.s.some(s=>!s.ex.length)).map(e=>e.w),
  noLevel: WORD_BANK.filter(e=>!e.ph && e.lv===null).map(e=>e.w),
  tooShort: WORD_BANK.flatMap(e=>e.s.flatMap(s=>s.ex)).filter(x=>x.en.replace(/\{\{|\}\}/g,"").split(/\s+/).length<9).length
})
```

`dup`、`emptySense`、`noLevel` 必須是空陣列，`noBlank` 必須是 0。
`total` 不可以比你動手前少——**少了就是資料檔壞掉了**，往下看。

**`tooShort` 是把關句子品質的硬指標**：你這批新增的字不可以讓它增加。
新寫的句子一律 10～14 字，寫完自己數一遍。使用者已經兩次特別要求句子的長度與品質，
短句（`The surgeon performed the operation.` 這種）等同沒有情境線索，是不合格的產出。

### ⚠️ 用腳本批次改資料檔的陷阱（踩過一次）

每個資料檔是一整個 JS 樣板字串，開頭 `addWords(\`` 、結尾 `` `); ``。
如果腳本用「一直讀到下一個 `@` 或空行為止」的方式抓取某個字的區塊，
遇到**檔案最後一個字**時會把結尾的 `` `); `` 也一起吃進去，
新內容被插到結束符號後面 → 整個檔語法錯誤 → `addWords()` 不執行 → 那個檔的字全部消失。

症狀是 console 出現 `SyntaxError: Unexpected token '='`，而 `WORD_BANK.length` 大幅減少。
批次腳本跑完後**務必檢查每個資料檔只有一個 `` `); `` 且位在最後一行**。

## 另一種常見要求：修正例句

使用者會從 App 的「設定 → 回報的怪句子」複製一份清單過來。
照清單找到對應的 `@單字` 底下那一句，直接改寫該行即可。

## 每日安排與學習紀錄（2026/08/31 加的）

- **每天的功課**：每天第一次開 App，`ensureToday()` 就照課表順序補上當天的新字
  （`S.autoLoad` 現在**預設開**，只有明確關過的人才會存成 `false`）。
  練習頁最上面那張卡片是**今天的進度＝新字＋到期複習，不含錯題**
  （`buildQueue("today")` 只取 `normalDue()`）。
  錯題是下面獨立的一段，可以一次清 `WRONG_BATCH`（20）題。
  ⚠ 別把錯題併回今天的進度：錯題本一旦積到幾百個，
  今天的功課就會顯示七百題，看起來像不可能的任務，但那是舊帳不是今天的量。
  `newToday()` 會把「自動載入的」與「自己在課表加的」取 max，
  所以手動加過一整組之後不會再被自動補一份、變成雙倍。
- **目標範圍**（`S.scope`，預設 `"15"` 也就是 1～5 級）：`targetWords()` 與
  `writtenInScope()` 是唯一的真相來源，課表的倒數、進度條、字庫缺口與紀錄頁的
  完成日推算全部走它們，不要再直接用 `OFF_COUNT`。
  `planUnits()` 也會把範圍外的級數整批推到課表最後面。
- **速度推算**（`paceInfo` / `paceHTML`）：算「平均一天幾個單字」與「幾天背完」。
  「一天練幾個字義」用 `w`，**含新字＋到期複習＋錯題**；完成日則只能用新字速度
  （`n`／`life`）推——複習再多也不會讓沒學過的字變少。兩者不要混用。
  分母一律是**日曆天**（沒練的日子也算），只除以有練的天數會過度樂觀。
  速度來源優先取最近 14 天的 `n`，但要有一半以上的日子有紀錄才採用
  （`n` 是這次改版才開始記的），否則退回「清單字義數 ÷ 從第一天到今天」。
  課表那頁算的是「要來得及每天該學幾個」（目標），這裡算的是現況，兩邊要一起看。
- **紀錄頁**（`drawStat`）只讀 `S.log`，一天一筆：
  `a` 題數、`c` 答對、`w` 練到的不重複字義、`n` 當天新加入的字義、
  `ms` 學習時間（毫秒）、`g` 當天排定的題數。
  舊存檔只有 `a` 與 `c`，缺的欄位一律當 0，**不需要轉檔**。
- **時間怎麼算**：只在「練習」與「錯題本」兩頁、且最近 90 秒內有動作才累加，
  每 5 秒記一次、30 秒才寫一次 localStorage（免得一直觸發雲端同步）。
  切走或關掉分頁會補存一次。
- ⚠ **在 `S` 裡加欄位就要同步改 `sync.js` 的 `merge()`**，
  它是逐欄位重建的，沒列到的欄位同步一次就被抹掉。
  這次已補上 log 的新欄位、`S.auto`（逐日取 max）與 `autoLoad`。

## 排程與負擔估算（2026/08/31 調整）

- **跳格規則**（`submit()` 裡的 `jump`）：首次無提示答對 → 跳 5 格（30 天），
  之後每次答對跳 2 格，答錯退 2 格，**用了字母提示答對只升 1 格**。
  ⚠ 提示那條原本是「完全不升級」，那是個 bug：`INT[0]` 是 0，
  box 停在 0 的字下次到期時間就是「現在」，於是它永遠停在熟練度 0、
  每一輪都再出現、還一直算進今天的進度。
  改成升一格（相當於 FSRS 的 Hard）之後，答對之後 box 至少是 1，
  不可能再出現零間隔。`fixStuckBox0()` 是給既有存檔的一次性修復。
  依據：Cepeda 2008「最佳間隔約為測驗延遲的 10～20%」（距考 144 天 → 二十幾天），
  以及 FSRS 的資料——一次成功複習讓穩定度成長約 3 倍，而階梯只有 2 倍，
  一格一格走會讓排程落後於實際記憶。
  用 FSRS-4.5 預設參數模擬：總題數 −22%，考試當天預期保留率 98.6% → 97.1%。
  跳 3 格也試過，保留率掉到 94.2%，不划算。
- **`Q_PER_UNIT` / `SEC_PER_UNIT`（5.0 題、71 秒）**：一個新字義從學到考前的總成本，
  課表與倒數的題數、分鐘都由 `estLoad()` 從這兩個常數算出來。
  ⚠ **改動跳格規則或 `INT` 階梯，一定要重跑模擬並更新這兩個數**，
  否則課表上的分鐘會失真（舊的「6.6 題 × 12 秒」低估了 20～25%，
  主因是沒把「答錯的題比較貴」算進去：答對約 10 秒，答錯要看答案與例句約 28 秒）。
- 模型只到「穩定之後」，所以課表另外會顯示 `actualLoadHTML()`——
  最近七天的實際題數與分鐘。兩者差很多時，相信實際的那個。

## 排序與畢業機制大改版（2026/09/05）

使用者提出五點，逐項查證後有一點是真缺陷、一點被文獻反對（後來又被他推翻）。

### 改了什麼

| 項目 | 改動 |
|---|---|
| **錯題回主線** | `normalDue()` 原本是 `filter(!i.wb)`——**答錯就被踢出今天的進度**，只能手動進錯題本。現在改成「今天錯的留在主線（10 分鐘後回來），跨天的舊帳才排除」，靠新欄位 `it.wbAt` 分辨 |
| **優先排序** | `buildQueue` 不再用 `shuffle()`。新的 `priority()`：今天錯過 +1000、逾期比例 ×10、累積錯誤 ×5，再加隨機抖動避免每天順序一樣 |
| **難度分流** | 跳格改看級數與錯誤次數。**級數只當第一次的先驗**（1～2 級 +6＝60 天、3～4 級 +5＝30 天、5 級 +4＝14 天），之後改用實測的 `wrong`（≥3 次改成一格一格走）。答錯退格 `wrong ≥ 5` 時退 3 格 |
| **「太簡單」兩次畢業** | 按鈕從作答**前**移到作答**後**，且只在「無提示答對」時出現。第 1 次按＝記下（`it.easy = 1`），30 天後換一句例句再答對並再按一次＝**畢業，寫進 `S.grad`，永久排除** |
| **錯題分兩段** | 首頁分「今天錯的 X 個（已在進度裡）」與「舊帳 Y 個（分批清）」，`todayWrong()` / `oldWrong()` |
| **考前抽考** | `btnSprint` 從 `S.grad` 與 `S.known` 抽 **10%** 回來考。重點不是抓回那幾個字，是**量出自我評估準不準**——抽考正確率 95%＋代表篩得準，70% 代表篩過頭了 |

### 為什麼「兩次就永久排除」是對的

一開始我引 Karpicke & Roediger (2008, Science)「答對一次後停止測驗會大幅降低保留率」
反對永久排除。**使用者反駁得對，我引用時漏了那個研究的前提**：它用的是受試者
完全沒學過的斯瓦希里語生字，穩定度接近零。`book`、`happy` 這種國小就會的字，
穩定度是十幾年累積的，套用那個結論是錯誤引用。

分界線不是「簡單／難」，是**「這個字的記憶是 App 給的，還是 App 之前就有的」**，
而 `seen === 1 && wrong === 0 && hinted === 0` 正是後者的直接證據。

而且**快篩才是三條路裡證據最弱的**（零次實際拼字就永久排除），卻一直被允許。
「太簡單」要拼對兩次＋自己判斷兩次，嚴格得多，沒有理由待遇更差。

停在兩次而不是三次：Rawson & Dunlosky 的資料顯示再往上加報酬遞減、
時間成本卻線性上升。

### ⚠ 一句例句的義項（全庫 48.8%）

第 2 次確認本來是靠 `sn.ex[it.seen % sn.ex.length]` **自動換一句**——
隔 30 天又換句子還答得出來，才算真的會。但將近一半的義項只寫了一句，換不了。

這種時候 `renderCard()` 會把**中文蓋起來**（`maskZh`），同一句少一個線索，
難度接近換句。偷看了就 `zhPeeked = true`、不給畢業（但不影響熟練度，
那跟字母提示是兩回事）。

### ⚠ 新增的狀態欄位

- `S.grad`（物件，key 是 `idOf(w, si)`）：畢業的**字義**。
  **是字義層級不是單字層級**——`bank` 的「銀行」畢業不代表「河岸」也畢業。
  **已同步加進 `sync.js` 的 `merge()`（取聯集）**，漏掉會讓畢業紀錄同步一次就被抹掉。
- `it.wbAt`：答錯那天的日期字串，用來分「今天錯的」與「舊帳」。
- `it.easy`：0／1，第一次「太簡單」確認過了沒。答錯或用提示會歸零。
- `isExcluded(w, si)`：快篩（整個字）與畢業（單一字義）的統一入口。

### 常數重算

改了跳格規則，依規定重跑模擬。⚠ **重跑的模型是簡化版，重現不了原本 5.4／76
的絕對值**（它算舊規則是 4.26 題／52.7 秒），所以只取**相對變化**套回原始基準：

    新／舊 ＝ 題數 0.924、秒數 0.929
    5.4 × 0.924 ＝ 5.0     76 × 0.929 ＝ 71

假設第 1、2 級七成、第 3 級三成、第 4／5 級一成的字會被按「太簡單」畢業。
使用者按得比這少的話，實際值會往 5.4／76 靠回去，兩週後用紀錄頁的實際數字校正。

### 改完之後的自我稽核找到 7 個 bug（同一天）

大改版寫完當下**看起來是對的**，實際造假存檔跑過才浮出來。這批全是
「數字看起來怪怪的」那一類，跟 2026/09/06 那次一樣，讀程式不容易發現。

1. **「到期要複習」變成負數**：答錯的字 `due` 是「10 分鐘後」，所以
   `todayWrong()` 裡只有一部分已經到期，但三項分解拿它**全額**去扣
   → `due 0 − 新字 0 − 今天錯的 4 = −4`。要用 `wrongDue`
   （`normalDue().filter(i => i.wb)`）去扣，等待中的另外列一行。
2. **導覽列徽章與錯題本頁算全部錯題，首頁與「清掉錯題」只算舊帳**：
   徽章寫 16、按鈕寫「清掉錯題（12 題）」，同一畫面兩個數字。
   `refreshHeader()` 的徽章、`todayTask()` 的 wrong、`drawWrong()`
   全部改成 `oldWrong()`——今天錯的已經在主線裡，列在那裡會讓人以為要清兩次。
3. **考前抽考加回來的字被當成「沒學過的新字」**：`seen: 0` 會被
   `drawDrillStart` 的 `!i.seen` 認成新字。改成 `seen: 1`，順帶讓
   `renderCard` 取下一句例句。
4. **常錯的字也能按「太簡單」永久排除**：`wrong ≥ 3` 是實測「這個字對你就是難」，
   跟「太簡單」自相矛盾。加了 `canGraduate()`，按鈕直接不顯示。
5. **舊帳的清理順序是「越舊越前面」**，跟 CLAUDE.md 原本記的意圖相反。
   `priority()` 對錯題改用「距離答錯過了幾天」而不是逾期比例——
   逾期比例會剛好排成相反的順序。
6. **`sync.js` 的 wb 聯集沒帶上 `wbAt`**：兩台裝置衝突時，
   合併後的字會變成 `wb: true` 但 `wbAt: ""` → 今天錯的被歸成舊帳。
7. **「含今天錯的 N 個」跟 1. 同源**，另外兩行也拿 `wrongToday` 去講
   「已經排進去的」。
9. **按「清除全部學習進度」會讓整個 App 當掉**（2026/09/07 稽核找到）：
   `btnWipe` 重建的 `S` 沒有 `grad` 欄位 → `isExcluded()` 讀 `S.grad[...]`
   直接 TypeError → `drawDrill()` 整頁白掉。重新整理才會被 `load()` 補回來。
   **在 `S` 加欄位時，`load()`、`btnWipe`、`sync.js` 的 `merge()` 三處都要改。**
   `isExcluded()` 已加 `|| {}` 保險。
8. **「太簡單」按鈕被例句區擠出畫面**（2026/09/07 使用者實際回報）：
   `#fb` 答完之後會塞進整個 `allExamplesHTML()`，實測 **1477px 高**，
   而 `rowSkip` 排在它後面 → 按鈕落在 y≈2769px，手機上永遠看不到。
   按鈕**存在、也有顯示**，純粹是 DOM 順序問題——這種 bug 用讀程式找不到，
   要量 `getBoundingClientRect()`。已把 `rowSkip` 移到 `#fb` 前面。

驗收方法（下次照做）：造一份 250～300 個字義、含各種狀態的假存檔，
**實際跑兩輪完整練習**（含答錯、用提示、按太簡單），每一頁都切過去看，
最後檢查「三項相加 === due」與「徽章 === 舊帳 === 清除按鈕的數字」。
`merge()` 在 IIFE 裡瀏覽器叫不到，用腳本把那段抽出來單獨用 node 測。

## 佇列穩定：待做量爆掉時暫停自動補新字（2026/09/07）

`loadTodayNew()` 原本**無條件**每天補滿額度，完全不看昨天做完沒有。
新字是整個系統唯一的「入流」，複習與錯題都是它的下游——
入流持續大於處理速度時，佇列會**無上界地成長**（Little's law）。

模擬（每天上 70 個新字）：

| 每天做得完 | 第 30 天待做 | 第 60 天待做 |
|---|---|---|
| 150 題 | 116 | **1282** ← 失控 |
| 200 題 | 114 | 133 |
| 250 題 | 114 | 133 |

加上 `newPaused()`（待做量 > 每日額度 × 2 就停自動補字）之後，
同樣 150 題／天的條件下第 60 天是 **88 題**。

**停的只有自動補字**，手動的「還是要載入 N 個」照樣可按——
要不要硬吃是使用者的決定，只是不再默默替他決定。

### ⚠ 負擔曲線是後重前輕，不要用前 30 天的體感估計

同一份模擬，每天上 70 個新字、額度不設限時的**實際每日題數**：

| 第 10 天 | 第 30 天 | 第 45 天 | 第 60 天 | 第 90 天 |
|---|---|---|---|---|
| 115 題 | 125 題 | **200 題** | 216 題 | 218 題 |

第 30 到 45 天之間幾乎翻倍——那是首波「跳 30 天」的字回來的時候。
使用者在第 30 天前後會突然覺得變重，那是正常的，不是壞掉。

### 可持續的新字量

| 每天做得完 | ≈ 分鐘 | 每天最多能上幾個新字 |
|---|---|---|
| 100 題 | 23 | 30 |
| 150 題 | 35 | 45 |
| 200 題 | 47 | 65 |
| 250 題 | 58 | 85 |
| 300 題 | 70 | 100 |

`perDay` 設得比這張表高，待做量就會一路長上去。

## 快篩與畢業要能反悔（2026/09/07）

`S.known`（快篩）與 `S.grad`（畢業）都是**永久排除**，而原本
**沒有任何取消的方法**——唯一的出口是「清除全部學習進度」，
等於為了救一個字砍掉全部進度。

快篩要連按一千多次，誤觸幾乎是必然的。已加：

- 快篩畫面的「**← 上一個（某字）按錯了**」：`scrHistory` 記錄每一次判斷，
  退回時把 `S.known` 刪掉並把那個字塞回佇列最前面。「不會」也能退。

## 錯題抽樣與畢業門檻（2026/09/09，使用者回報後修）

### 舊帳永遠只練到同一批（starvation）

`buildQueue("wrong")` 原本是「照優先序取前 N 名」。優先序對錯題是
「距離答錯過了幾天」，隨機抖動只有 ±4，遠小於分數差距——
**實測 60 個舊帳、每批 20 題，連續三次抽到完全相同的 20 個**，
後面 40 個一次都沒被練到。而且答錯的隔天又變成最新的舊帳，同一批一直循環。

改成**分層抽樣**：照優先序排好後取前段當候選池（批量的 3 倍），
再從池子裡隨機抽。修好後第 1 次與第 2 次的重複從 20/20 降到 **8/20**，
五批碰到 **55／60** 個不同的字。

⚠ 另一半原因沒辦法用程式修：**48.8% 的義項只有一句例句**，
同一個字再出現時題目就是同一句（`sn.ex[it.seen % sn.ex.length]` 轉不動）。
要根治只能回頭補第二句例句。

### 「太簡單」按鈕無聲消失

`canGraduate()` 原本是 `wrong < 3`，實測**44% 的「答對」都會被擋掉**，
而且是**整顆按鈕不顯示**，看起來就是壞掉（使用者回報兩次）。兩個修正：

1. **放行條件加上 `box >= 5`**：撐過 30 天的間隔還答得出來，就是
   「現在對你已經不難」的直接證據。舊的錯誤次數是**先驗**、
   目前的穩定度是**實測**，有實測就該蓋過先驗——跟跳格規則用級數當先驗、
   用 `wrong` 當實測是同一個原則。
2. **被擋掉時要說明理由**，顯示成 disabled 的
   「這個字錯過 N 次，先練到熟練度 5 才能標『太簡單』」。
   **答對了卻沒有按鈕，一定要講原因**，無聲消失一定會被當成 bug。

## 已經修掉的 bug（2026/09，找的時候可以當範例）

1. **用提示答對的字永遠停在熟練度 0**：`submit()` 裡「用提示不升級」是整段跳過
   box 的計算，但下一行 `it.due = Date.now() + INT[it.box]` 照跑，`INT[0]` 是 0
   → 立刻又到期 → 每一輪都再出現、永遠 0 級。改成升 1 格；`fixStuckBox0()`
   修既有存檔（使用者實際被卡住 113 個字）。
2. **答完切走再切回來會重問同一題**：`drawDrill()` 無條件把 `answered` 設成
   false，而答完的那題還在 `queue[0]`，於是同一次作答算兩次、box 連跳兩次。
   現在 `if (answered && qCur === queue[0]) queue.shift();`。
3. **標題列的「待複習」用 `dueItems()`（含錯題）**，跟練習頁卡片的
   `normalDue()`（不含錯題）對不起來，同一畫面兩個數字。已統一成 `normalDue()`。
4. **`loadTodayNew()` 會把進行中的那一輪 `queue` 清掉**：從錯題本按「重練這組」
   （`go("drill")` 會先跑 `ensureToday()`）、跨過午夜、或在課表調高每日額度之後
   回練習頁，整輪會直接作廢跳回起始畫面。現在只有 queue 是空的時候才清。

5. **練習頁把新字算了兩次**（2026/09/06 收尾稽核找到）：卡片寫「還有 324 題」，
   底下卻列「新字 70 個」＋「到期複習 324 個」，看起來像 394 題。
   新字加進清單時 `due` 就是「現在」，所以它們本來就在 `normalDue()` 裡面。
   現在用 `seen === 0` 認新字，兩個數字必定加起來等於 due，畫面上也直接寫明。
6. **答錯的字被踢出主線，再也不會自己回來**（2026/09/05 修）：`normalDue()` 是
   `dueItems().filter(!i.wb)`，答錯後 `wb=true` 就永遠出不了錯題本。它 10 分鐘後
   就到期了，主線卻不看它。等於對「你最不熟的那批字」實施了 Karpicke & Roediger
   實驗裡保留率最差的處理。見上面「排序與畢業機制大改版」。
7. **快篩按「不會」會讓今天的進度爆量**：`renderScreenCard()` 的
   「不會，加入練習」是 `addItem()`，due 設成「現在」——快篩五百個字按一百次「不會」，
   今天就從 70 題變三百多題。快篩本來是要*減少*負擔的動作。
   現在改成什麼都不做（那個字沒被標進 `S.known`，本來就還在 `planUnits()` 裡），
   按鈕改叫「不會，照課表排」。
7. **義項被刪掉的字會變成清不掉的幽靈題**：`renderCard()` 遇到 `senseOf()` 是 null
   只 `queue.shift()`，但 `normalDue()` 只檢查單字在不在字庫、不檢查義項編號，
   於是那筆每輪都算進「今天的進度」卻永遠答不到。現在一併 `delItem()`。

⚠ 這幾個都是「數字看起來怪怪的」那一類，靠讀程式不容易發現，
**要在瀏覽器裡實際跑一次流程去驗**（buildQueue → submit → go 切頁 → 再看狀態）。
第 5～7 個是 2026/09/06 用「造一份 1400 個字義的假存檔再逐頁看數字」找出來的，
那個方法很有效，下次稽核可以照做。

## 已經清掉的死碼（2026/09/06）

- **`it.st`**：舊的「答對兩次才畢業」規則留下的欄位，三處寫入、零處讀取。已移除。
- **`applyPlan2608()`**：2026/08/31 的一次性設定遷移，使用者已確認設定無誤，整段刪掉。
  `sync.js` 的純量清單裡還留著 `plan2608` 這個鍵，無害（函式沒了就不會再套用），
  下次順手整理時可以一起拿掉。
- **`loadTodayNew()` 裡的 `if (!queue.length) queue = [];`**：條件成立時做的事等於沒做。
  行為本來就是對的（永遠不清空進行中的那輪），只是留著修 bug 的痕跡，已改成註解。
- `fixStuckBox0()` **保留**：它是資料修復，任何還沒跑過的裝置同步進來時仍需要它。

## 不要動的東西

- `app.js` 的 localStorage key 是 `vocabApp_v1`。改動存檔結構會**清掉使用者的學習進度**，
  除非使用者明確要求，否則不要改；真要改請一併寫升級轉換邏輯。
- `data/bank.js` 的解析器格式不要改，所有資料檔都依賴它。

## 視覺改版（2026/09/07）

原本整套是 **Tailwind 預設色（blue-600 ＋ 冷灰）＋ 系統字 ＋ 14px 圓角 ＋ 柔陰影**——
那正好是「AI 生成」的標準長相。改成有觀點的一套，每一項都有理由：

| 改動 | 理由 |
|---|---|
| **紙色底 ＋ 暖黑墨字**（`--bg:#f4f2ed`／`--text:#22201d`） | 暖中性色的喚起度比冷灰低。這是每天看 60～90 分鐘的工具，高彩度會累積視覺疲勞 |
| **單一低彩度墨藍**（`#2b4c7e`） | 孤立效應（von Restorff）要成立，前提是畫面上**只有一個**東西被強調。原本每張卡都有飽和藍，等於沒有重點 |
| **去陰影，只留髮絲線** | 陰影一多就變成「玻璃卡片飄在灰底上」，那是模板長相。紙感靠邊界，不靠浮起 |
| **圓角 14 → 10／11 → 8** | 大圓角是 App 感；收斂後偏印刷品 |
| **例句改襯線體**（`--serif`） | 內容與介面該有不同的「聲音」：那句英文是要研讀的**材料**，不是按鈕文字。原本的 `ui-rounded` 圓體讀起來像裝飾字 |
| **例句行距 1.95 → 1.62** | 19px × 1.95 ＝ 37px 行距，換行後兩行離太遠，依 Gestalt 接近性會被讀成兩件事而不是一句話 |
| **區段標題後面拉一條線** | 版面書的做法，比「粗體字浮在空白裡」有結構 |

### 三層互動層級（這次真正修好的）

之前 `#btnGo` 只把 padding 從 14 縮到 12px 就想翻轉層級，**那沒有用**——
畫面上實心色塊永遠壓過線框，縮 2px 看不出差別。現在分成三層：

1. **輸入框**＝白底 ＋ 內凹陰影 ＋ 粗邊（凹進去的**槽**）
2. **送出**＝淡色實底（凸出來的**塊**）
3. **提示／不會**＝線框（ghost）

靠**形狀**而不是顏色區分，比兩個都塗成淡藍可靠。
⚠ 深色模式要反過來：亮色靠「比背景亮」表示凹陷，暗色靠「比背景暗」，
所以 `#ansIn` 在深色模式另外指定 `background:var(--bg)`。

### 紅色的用法

`nav .badge` 與 `.btn.bad`（清錯題）**都不再用飽和紅**。

錯題本裝的是「累積下來的舊帳」，照設計定位就不該製造壓力；
而且天天亮著、又不需要立刻處理的紅點會**習慣化**而失去訊號價值，
剩下的只有每天開 App 被嚇一次。紅色留給**答錯那張卡的回饋**——
那才是真正的錯誤，而且是一次性的。

### 減字

依 Sweller 的外在認知負荷與**冗餘效應**（同一份訊息同時用多種形式呈現會傷害理解）：

- 按鈕不再重複題數（大字就在正上方 40px 處）
- 首頁「錯題」原本兩張卡在講同一件事，併成一張
- 按鈕下方那段四行說明砍成一行

⚠ **三項分解（新字／到期複習／今天答錯）不能砍**，那是防止
「還有 324 題」底下寫著「新字 70 ＋ 到期複習 324」那種誤會的載重結構。

## 介面設計的幾個決定（2026/09/06）

- **輸入框才是主角**：`#ansIn` 平常就帶一圈淡主色邊、對焦時再加光暈，
  而答題卡上的送出鈕（`#btnGo`）刻意做小一號。
  原本的層級是反的——最搶眼的是那顆整寬飽和藍按鈕，但真正要動作的是輸入框。
- **底線旁邊標字母數**（`.blank .bn`）：底線的數量是分辨同義詞
  （trash 5 ／ garbage 7）唯一的線索，但 3 個和 5 個底線用看的數不出來，
  所以直接標一個小數字。**片語不標**，那會變成「所有單字加起來的字母數」，只會誤導。
- **熟練度圓點改成空心＋外框**：原本未填滿的點用 `--line` 當底色，在白卡片上幾乎隱形。
- **導覽列用單色線條 SVG，不用 emoji**：emoji 在不同手機上長相不一致，
  而且沒辦法跟著 `--accent` 變色。圖示吃 `currentColor`，選中時自動變主色。
  ⚠ 小尺寸（21px）下要選「一眼認得出來」的造型——
  齒輪畫成圓心加放射線會變成太陽，所以設定用的是滑桿。

## 本機預覽

`.claude/launch.json` 已設定好，用 preview_start 啟動名為 `vocab` 的設定即可
（`python -m http.server 8712`）。
