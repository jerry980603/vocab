# 大考中心的詞彙表 PDF 只授權「非營利目的使用」，公開轉載需另外取得同意。
# 它只是用來查證單字分級的參考資料，App 本身不需要它，所以不要上傳到 GitHub。
*.pdf

# 本機權限設定，換台電腦不適用
.claude/settings.local.json

# 注意：.claude/ 底下其他東西（skills/、launch.json）要跟著上傳，
# 這樣換電腦或重新 clone 之後，「繼續補單字」的流程才還在。
